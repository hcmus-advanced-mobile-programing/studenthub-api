export enum UserRole {
  MANAGER = 'MANAGER',
  ADMIN = 'ADMIN',
  COMPANY = 'COMPANY',
  STUDENT = 'STUDENT',
  USER = 'USER',
}

export enum StatusFlag {
  Waitting = 0,
  Offer = 1,
  Hired = 2,
}

export enum DisableFlag {
  Enable = 0,
  Disable = 1,
}

export enum TypeFlag {
  Working = 0,
  Archieved = 1,
}
